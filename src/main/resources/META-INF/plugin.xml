<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.phpfunctioninjectioncompanion</id>
    <name>PHP Function Injection Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on <code>call_user_func(</code> or
        <code>call_user_func_array(</code> whose function-name
        argument directly references a PHP superglobal
        (<code>$_GET</code>, <code>$_POST</code>,
        <code>$_REQUEST</code>, <code>$_COOKIE</code>). This is the
        textbook Function Injection anti-pattern: OWASP's own guidance
        documents that "when an attacker passes a function name with
        user input to call_user_func, it can lead to remote code
        execution by passing the system function with arbitrary
        commands" -- an attacker who controls the callable name can
        invoke any built-in function reachable by that call.</p>
        <p>Confirmed real gap: "PHP Inspections (EA Extended)" (one of
        the most widely used PHP inspection plugins on Marketplace)
        does not cover function injection detection anywhere in its
        documented security feature list -- confirmed by reading it
        before building this.</p>
        <p><b>100% static text analysis</b> -- a regex-based line
        scanner, not a real PHP parser, so it works whether the PHP
        plugin is installed or not.</p>
        <p>Stated honestly: v0.1 only flags a superglobal referenced
        within the call's own parentheses on the same line. A function
        name built from an intermediate variable assigned from a
        superglobal several lines earlier isn't traced (real data-flow
        analysis, out of scope for a text scanner).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="PHP Function Injection Companion"
            displayName="Function name built from a superglobal"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.phpfunctioninjectioncompanion.inspection.FunctionInjectionInspection"/>
        <notificationGroup id="PHP Function Injection Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
